# openspec/config.yaml
# Auto-generated by sdd-init — 2026-06-30
schema: spec-driven

context: |
  React 19 + Vite 8 + Tailwind CSS v4 + React Router v7 (client-side SPA).
  Bun package manager. oxlint linter.
  State management via Context + useReducer (CartContext, AuthContext).
  DummyJSON for auth, localStorage cart persistence, simulated checkout.
  JSX only — no TypeScript.
  No test infrastructure exists.

rules:
  proposal:
    - Include rollback plan for risky changes
    - One proposal per change scope
  specs:
    - Use Given/When/Then for behavioral scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Reference existing ADRs from docs/ARCHITECTURE.md
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns (JSX, context hooks, Tailwind classes)
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: "bun run build"
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas
    - Update docs/ when specs change

testing:
  strict_tdd: false
  runner: null
  layers:
    unit: false
    integration: false
    e2e: false
  coverage: false
  quality:
    linter: oxlint
    type_checker: null
    formatter: null
